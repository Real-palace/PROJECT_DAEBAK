@{
    ViewData["Title"] = "Access Denied";
}

<div class="min-h-screen flex items-center justify-center bg-gray-100">
    <div class="bg-white p-8 rounded-lg shadow-md max-w-md w-full text-center">
        <div class="mb-6">
            <svg class="mx-auto h-16 w-16 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
        </div>
        <h1 class="text-2xl font-bold text-gray-800 mb-3">Access Denied</h1>
        <p class="text-gray-600 mb-6">You do not have permission to access this resource.</p>
        <div class="space-y-3">
            <a href="@Url.Action("AdminPage", "Account")" class="block w-full bg-primary text-white py-2 px-4 rounded hover:bg-primary/90">
                Return to Dashboard
            </a>
            <a href="@Url.Action("Logout", "Account")" class="block w-full bg-gray-200 text-gray-700 py-2 px-4 rounded hover:bg-gray-300">
                Logout
            </a>
        </div>
    </div>
</div>